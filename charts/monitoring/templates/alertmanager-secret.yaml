# =============================================================================
# Secret pour AlertManager - Mot de passe SMTP Gmail
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-smtp-secret
  namespace: monitoring
  labels:
    app: alertmanager
type: Opaque
stringData:
  # Gmail App Password (sans espaces)
  smtp-password: "nlllkwmzichvhbdq"

